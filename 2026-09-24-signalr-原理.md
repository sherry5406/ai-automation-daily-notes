# Angular × SignalR 前端教學｜2026-09-24

## 今日主題：先搞懂 SignalR 到底在做什麼

今天先不急著寫很多 Angular 程式。

我們先把一件事搞懂：**SignalR 為什麼可以讓後端主動把資料推到前端？**

後面要做的「即時通知、Session Activity、單一頁籤、閒置登出」都建立在這個觀念上。

---

## 1. 今天學什麼？

今天只學一個核心觀念：

> **一般 API 是前端問後端；SignalR 是建立一條長時間存在的連線，讓後端需要時可以主動通知前端。**

先不要管重連、單一頁籤、閒置登出。

今天只要能分清楚「HTTP API」和「SignalR」的角色，就算完成。

---

## 2. 白話原理

### 一般 API

例如使用者登入後，Angular 想知道 Session 狀態：

```text
Angular
  │
  │ GET /api/session
  ▼
Backend
  │
  │ 回傳 Session 資料
  ▼
Angular
```

前端要「主動問」才有答案。

如果前端每 5 秒問一次，就是 Polling：

```text
前端 ──問──> 後端
前端 <──回── 後端

5 秒後

前端 ──又問──> 後端
```

這種方式簡單，但即時性與請求數量要另外考量。

### SignalR

SignalR 的想法不太一樣：

```text
Angular
   │
   │ 建立 SignalR 連線
   ▼
SignalR Hub
   │
   │ 連線維持著
   │
   ├──── 後端有事件 ────> Angular
   │
   ├──── 後端有通知 ────> Angular
   │
   └──── Session 狀態變更 ─> Angular
```

所以你可以把它想成：

> **HTTP API 像打電話問：「現在狀況如何？」**
>
> **SignalR 像保持通話：「有事情你直接跟我說。」**

SignalR JavaScript Client 可以建立 `HubConnection`，也可以監聽連線生命週期；目前 ASP.NET Core SignalR JavaScript Client 預設不會自動重連，需要明確設定 `withAutomaticReconnect()`。citeturn0search17

---

## 3. 實際情境：登入後 Session Activity

假設你的企業系統有這個需求：

> 使用者登入後，如果長時間沒有有效操作，就要登出回首頁。

後端已經有 SignalR Hub。

前端 Angular 可能負責：

```text
使用者登入
   ↓
Angular 建立 SignalR 連線
   ↓
Backend 知道這個 Client 已連線
   ↓
使用者操作畫面
   ↓
Angular 回報有效 Activity
   ↓
Backend 更新 LastActivityAt
```

如果後端判定 Session 已經過期，可以透過 SignalR 告訴前端：

```text
Backend
  │
  │ SessionExpired
  ▼
Angular
  │
  ├─ 顯示提示
  ├─ 清除登入狀態
  └─ 回登入頁
```

這就是 SignalR 很適合做的事情：**後端狀態發生變化時，主動通知前端。**

---

## 4. Angular 純前端要負責什麼？

既然你的後端 SignalR 已經寫好了，前端不用重新設計 Hub。

Angular 最主要負責 4 件事情：

```text
① 建立連線
② 接收事件
③ 更新前端狀態
④ 管理連線生命週期
```

實務上我會建議把 SignalR 放進 Angular Service，而不是每個 Component 都自己建立一條連線。

Angular Service 很適合集中管理共用邏輯；官方文件也將 Service 定位為可以被多個 Component 共用的程式碼與商業邏輯。citeturn0search6

概念上：

```text
                 ┌──────────────┐
                 │ SignalRService│
                 └──────┬───────┘
                        │
             ┌──────────┼──────────┐
             ▼          ▼          ▼
          Header     Dashboard   Session
```

這樣可以避免：

```text
Component A → 建立 SignalR
Component B → 又建立 SignalR
Component C → 再建立 SignalR
```

最後同一個頁籤裡可能出現多條不必要的連線。

---

## 5. 今天先看一個最小範例

假設後端 Hub URL 是：

```text
/api/hubs/session
```

Angular Service 可以先長這樣：

```ts
import { Injectable, signal } from '@angular/core';
import * as signalR from '@microsoft/signalr';

@Injectable({ providedIn: 'root' })
export class SignalRService {
  private readonly connection = new signalR.HubConnectionBuilder()
    .withUrl('/api/hubs/session')
    .build();

  readonly connected = signal(false);

  async connect(): Promise<void> {
    this.connection.on('SessionExpired', () => {
      console.log('Session 已過期');
    });

    await this.connection.start();
    this.connected.set(true);
  }
}
```

這段程式現在先不用全部背起來。

只看這 3 個地方：

### ① 建立 HubConnection

```ts
new signalR.HubConnectionBuilder()
```

意思就是：我要建立一個 SignalR Client 連線。

### ② 指定後端 Hub

```ts
.withUrl('/api/hubs/session')
```

這裡就是接你的後端 SignalR Hub。

實際 URL 要以你的後端規格為準。

### ③ 監聽後端事件

```ts
this.connection.on('SessionExpired', () => {
  console.log('Session 已過期');
});
```

意思是：

> 後端如果送來 `SessionExpired`，Angular 就執行這段程式。

SignalR Client 的事件名稱必須跟後端實際送出的事件名稱一致。

---

## 6. Angular Signal 的角色

這裡可以先把 Signal 想成：

> 「SignalR 收到的新狀態，放到 Angular Signal 裡，畫面就可以跟著更新。」

例如：

```ts
readonly connected = signal(false);
```

連線成功：

```ts
this.connected.set(true);
```

畫面就可以讀：

```html
@if (connected()) {
  <span>即時連線中</span>
} @else {
  <span>尚未連線</span>
}
```

Angular 官方也提供 Signals 與 RxJS 互通的 API，因此未來如果你的 SignalR 封裝採 Observable，再轉成 Signal 也是可以的。citeturn0search3

但今天先不要加入 RxJS。

**先把 SignalR → Angular Service → Signal → Component 這條路線搞懂。**

---

## 7. 今天先不要做「單一頁籤」

這點很重要。

你最後想完成的是：

> **一個裝置，只允許一個頁籤使用這個系統。**

但今天先不要做。

因為它其實是另一個問題：

```text
SignalR 連線
     ↓
如何知道是不是同一台裝置？
     ↓
如何知道是不是另一個 Tab？
     ↓
誰是目前有效 Tab？
     ↓
另一個 Tab 要不要被踢掉？
```

這些會涉及瀏覽器 Tab 間協調，以及後端 Session 規則。

我們之後再一步一步做。

---

## 8. 今天也先不要做「閒置登出」

你目前的需求可以拆成兩層：

```text
使用者操作
    ↓
Angular 偵測有效 Activity
    ↓
SignalR 通知 Backend
    ↓
Backend 更新 LastActivityAt
```

另一條：

```text
Backend 判定 Session 過期
    ↓
SignalR 推送事件
    ↓
Angular 收到 SessionExpired
    ↓
清除狀態
    ↓
回登入頁
```

所以「閒置登出」不是單純靠前端 `setTimeout()`。

真正的 Session 最終判定應該由後端負責，前端主要負責偵測有效操作、同步 Activity，以及收到結果後處理 UI。

這也是後面我們要慢慢建立的完整架構。

---

## 9. 今天練習

不用寫完整功能，只回答下面 3 題：

### Q1
一般 API 與 SignalR 最大的差別是什麼？

### Q2
為什麼 SignalR 比較適合做「後端主動通知前端」？

### Q3
如果整個 Angular App 都共用同一條 SignalR 連線，應該比較適合放在哪裡？

建議答案方向：

```text
Q1：前端主動請求 vs 持續連線後由後端主動通知
Q2：因為連線建立後，Server 可以透過 Hub 主動推送事件
Q3：Angular Service
```

---

## 10. 常見踩雷

### ❌ 每個 Component 都建立一條 SignalR

```text
Header → connection 1
Dashboard → connection 2
Session → connection 3
```

容易造成重複連線與管理困難。

### ❌ 把 SignalR 當成 HTTP API

SignalR 不是：

```ts
await http.get('/api/session');
```

它是長時間維持的即時連線模型。

### ❌ 以為 SignalR 預設會自動重連

ASP.NET Core SignalR JavaScript Client 預設不會自動重連；要使用 `withAutomaticReconnect()` 等方式設定。citeturn0search17

### ❌ 把 Session 最終判定全部放在前端

前端計時器可以協助偵測使用者操作，但真正的 Session 是否過期，仍應以後端的 Session 規則為準。

---

## 11. 今天學會什麼？

今天只記住這張圖：

```text
                  HTTP API
Angular ─────────────────────> Backend
       <─────────────────────
             前端主動問


                  SignalR
Angular <══════════════════════> Backend
          持續連線
          Server 可以主動通知
```

再記住 Angular 的角色：

```text
Backend SignalR Hub
        ↓
HubConnection
        ↓
Angular Service
        ↓
Signal
        ↓
Component / UI
```

這就是我們後面所有實作的基礎。

---

## 下一步

**2026-09-25：Angular × SignalR｜第一步：建立 HubConnection**

下一篇才開始正式進入程式碼，會把：

```text
HubConnectionBuilder
.withUrl()
.build()
.start()
```

一個一個拆開講，並開始建立一個真正可以放進 Angular 專案的 `SignalRService`。

---

## 官方參考

- Microsoft Learn：ASP.NET Core SignalR JavaScript Client citeturn0search17
- Microsoft Learn：SignalR Configuration citeturn0search0
- Angular：Dependency Injection / Services citeturn0search1turn0search6
- Angular：RxJS ↔ Signals Interop citeturn0search3
