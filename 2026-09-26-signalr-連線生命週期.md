# Angular × SignalR｜連線生命週期｜2026-09-26

> 今天只學一件事：**SignalR 斷線時，Angular 前端到底會經過哪些狀態？**

## 今天學什麼

昨天我們已經知道 `HubConnection` 是前端和 SignalR Hub 之間的「電話線」。

今天先不做複雜功能，只學懂 3 個事件：

- `onreconnecting`：正在重新連線
- `onreconnected`：重新連線成功
- `onclose`：連線正式關閉

Microsoft 官方文件指出，`withAutomaticReconnect()` 不會預設啟用；啟用後，連線中斷時會先進入 `Reconnecting`，成功後回到 `Connected`，如果重連失敗到停止嘗試，才會進入 `Disconnected` 並觸發 `onclose`。預設重連等待時間為 0、2、10、30 秒。 citeturn0search0turn0search9

## 白話原理

把 SignalR 想成公司電話：

```text
Connected
   ↓ 網路突然斷掉
Reconnecting
   ↓
   ├─ 成功 → Connected
   │
   └─ 一直失敗 → Disconnected
```

所以「斷線」不一定代表「真的斷掉了」。

例如使用者正在公司系統填資料，Wi-Fi 短暫斷線：

```text
10:00  已連線
10:01  Wi-Fi 短暫斷線
10:01  Reconnecting
10:01  重新連線成功
10:01  Connected
```

這時候通常不需要把使用者踢回登入頁。

## 實際情境：企業系統通知

假設你的系統有一個右上角通知鈴鐺：

```text
🔔 已連線
```

網路暫時斷掉：

```text
🟡 連線中...
```

重新連線成功：

```text
🟢 已連線
```

真的無法恢復：

```text
🔴 即時功能已中斷，請重新整理頁面
```

這就是連線生命週期最常見的 UI 用法。

## Angular 純前端實作

建議把 SignalR 集中放在 Service，不要每個 Component 都自己建立一條 Connection。

Angular Service 很適合集中管理共用的連線與商業邏輯；使用 root scope 時可以讓整個 Angular App 共用同一個 Service instance。 citeturn0search8

```ts
import { Injectable, signal } from '@angular/core';
import {
  HubConnection,
  HubConnectionBuilder,
  HubConnectionState,
} from '@microsoft/signalr';

@Injectable({ providedIn: 'root' })
export class SignalrService {
  private connection?: HubConnection;

  readonly status = signal<HubConnectionState>(
    HubConnectionState.Disconnected,
  );

  async connect(): Promise<void> {
    this.connection = new HubConnectionBuilder()
      .withUrl('/your-hub-url')
      .withAutomaticReconnect()
      .build();

    this.connection.onreconnecting(() => {
      this.status.set(HubConnectionState.Reconnecting);
    });

    this.connection.onreconnected(() => {
      this.status.set(HubConnectionState.Connected);
    });

    this.connection.onclose(() => {
      this.status.set(HubConnectionState.Disconnected);
    });

    await this.connection.start();
    this.status.set(HubConnectionState.Connected);
  }
}
```

### 為什麼用 Angular Signal？

因為 UI 很適合直接反映「目前連線狀態」。Signal 值改變後，讀取它的 Angular UI 可以跟著更新。Angular 官方也建議 Signals 用來描述會變動的 reactive state；`effect()` 比較適合拿來同步非 reactive API，而不是拿來把一個 signal 複製到另一個 signal。 citeturn0search2turn0search12

所以今天先用：

```ts
status = signal(HubConnectionState.Disconnected);
```

不要急著加入 `effect()`。

## Component 怎麼使用？

```ts
readonly signalr = inject(SignalrService);
```

Template：

```html
@if (signalr.status() === HubConnectionState.Connected) {
  <span>🟢 已連線</span>
}

@if (signalr.status() === HubConnectionState.Reconnecting) {
  <span>🟡 重新連線中...</span>
}

@if (signalr.status() === HubConnectionState.Disconnected) {
  <span>🔴 已中斷</span>
}
```

實務上可以再把狀態轉成自己的 enum / view model，避免 Component 到處依賴 SignalR library 的 enum。

## 一個很重要的觀念：start() 失敗 ≠ 自動重連

這個地方非常容易踩雷。

`withAutomaticReconnect()` 主要處理「已經建立連線之後」的重新連線；**第一次 `start()` 就失敗時，不會自動幫你重試**。第一次啟動失敗仍要自己處理。 citeturn0search0

所以：

```ts
try {
  await this.connection.start();
} catch (error) {
  console.error('SignalR 初次連線失敗', error);
}
```

今天先知道這件事即可，不急著做完整 retry 機制。

## 今天練習

請自己想像這個情境：

> 使用者正在企業 EIP 系統裡工作，SignalR 是用來接收即時通知。

設計 3 個 UI 狀態：

```text
Connected     → 顯示「即時通知正常」
Reconnecting  → 顯示「網路不穩，重新連線中」
Disconnected  → 顯示「即時功能中斷」
```

先不要做「閒置登出」。

因為 **SignalR 斷線 ≠ 使用者閒置**，這兩件事情後面會分開學。

## 常見踩雷

### 1. 每個 Component 都 new 一個 HubConnection

容易造成：

```text
頁面 A → Connection 1
頁面 B → Connection 2
Header → Connection 3
```

後面做「單一裝置、單一頁籤」時會很難管理。

### 2. 看到 Reconnecting 就立刻 logout

不建議。

網路短暫斷線很正常，`Reconnecting` 本來就是給前端處理暫時性的連線中斷。 citeturn0search0

### 3. 把 SignalR 連線狀態當成 Session 狀態

這兩個概念要分開：

```text
SignalR Connected
        ≠
使用者 Session 一定有效
```

Session 是否有效，最終仍然應該以後端的認證與 Session 規則為準。

## 今天你只要記住 3 個事件

```text
onreconnecting
      ↓
onreconnected
      ↓
onclose
```

更精確地說：

```text
Connected
   ↓
網路中斷
   ↓
onreconnecting
   ↓
成功 → onreconnected → Connected
   ↓
失敗到停止重試 → onclose → Disconnected
```

## 下一步

下一篇進入：

**Angular × SignalR｜接收後端事件**

會開始真正做：

```ts
connection.on('ReceiveNotification', data => {
  // 收到後端推播
});
```

到那時候就會從「知道 SignalR 怎麼連線」正式進入「前端怎麼收到後端推播」。

## 官方參考

- Microsoft Learn：ASP.NET Core SignalR JavaScript Client
- Microsoft Learn：HubConnection API
- Angular 官方：Signals / Effects
